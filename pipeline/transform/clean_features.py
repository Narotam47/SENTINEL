"""
Phase 2 — reads secom_raw, cleans and engineers features, writes secom_features.
Stub for Phase 1; filled out in Phase 2.
"""
import logging
log = logging.getLogger(__name__)


def run():
    log.info("Transform pipeline not yet implemented — build in Phase 2.")


if __name__ == "__main__":
    logging.basicConfig(level="INFO")
    run()
